import 'package:flutter/material.dart';

class OrderLastScreen extends StatelessWidget {
  const OrderLastScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}